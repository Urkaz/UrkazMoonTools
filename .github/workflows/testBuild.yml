name: Test Build

on:
  workflow_dispatch:
    push:
    branches:
      - '1.20.6'
    paths:
      - 'common/**'
      - 'fabric/**'
      - 'forge/**'
      - '**.gradle'
      - '**.properties'


jobs:
  build_1_20_6:
    name: Build 1.20.6
    runs-on: ubuntu-latest
    steps:
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'oracle'
          java-version: 21
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: '1.20.6'
      - name: Build
        run: ./gradlew build --stacktrace
